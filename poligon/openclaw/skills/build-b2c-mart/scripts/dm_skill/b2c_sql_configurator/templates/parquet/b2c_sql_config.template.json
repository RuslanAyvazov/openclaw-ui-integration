{
  "appConfig": {},
  "treadConfigs": [
    {
      "name": "stg",
      "stages": [
        {
          "config": {"filegsql": ${rendered_template.stg.template.sql}},
          "enabled": true,
          "name": "PreStage"
        }
      ]
    },
    {
      "name": "historicity",
      "stages": [
        {
          "config": {"filegsql": ${rendered_template.historicity.template.sql}},
          "enabled": true,
          "name": "PreStage"
        }
      ]
    },
    {
      "name": "dqc_stg",
      "stages": [
        {
          "config": {
            "filedqc": ${rendered_template.dqc.template.json},
            "sparkEngineParams": {},
            "useKKD": false
          },
          "enabled": true,
          "name": "dataQualityCheck"
        }
      ]
    },
    {
      "name": "move_table",
      "stages": [
        {
          "config": {"filegsql": ${rendered_template.move_table.template.sql}},
          "enabled": true,
          "name": "PreStage"
        }
      ]
    },
    {
      "name": "coalesce",
      "stages": [
        {
          "config": {"filegsql": ${rendered_template.coalesce.template.sql}},
          "enabled": true,
          "name": "PreStage"
        }
      ]
    }
  ],
  "treadRelations": [
    {"from": "stg", "to": "historicity"},
    {"from": "historicity", "to": "dqc_stg"},
    {"from": "dqc_stg", "to": "move_table"},
    {"from": "move_table", "to": "coalesce"}
  ]
}
